{
  "version": 2,
  "builds": [
    {
      "src": "index.js",
      "use": "@now/node"
    }
  ],
  "routes": [
    {
      // Specify which paths will route to a destination using a regex
      // This one sends all paths to index.js for simplicity...
      "src": "/(.*)",
      // Specify the paths' destination
      "dest": "index.js"
    }
  ]
}